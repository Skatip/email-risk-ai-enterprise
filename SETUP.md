# Hosted Setup

Use `DEPLOYMENT.md` for the Vercel, Render, Neon PostgreSQL, Google OAuth, Supabase Storage, Sentry, and OpenAI setup. End users do not configure keys or token files; they connect Gmail through the product UI.
